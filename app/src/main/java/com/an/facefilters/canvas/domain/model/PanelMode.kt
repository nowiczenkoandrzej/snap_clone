package com.an.facefilters.canvas.domain.model

enum class PanelMode {
    ELEMENTS, PENCIL, TEXT, IMAGE, ASPECT_RATIO, FILTERS
}

enum class CanvasMode {
    DEFAULT, CROP, PENCIL, CREATE_STICKER, RUBBER
}